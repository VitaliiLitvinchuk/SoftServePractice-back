services:
  db:
    image: postgres:latest
    container_name: SoftServeDB
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: softserve
    ports:
      - "5555:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
